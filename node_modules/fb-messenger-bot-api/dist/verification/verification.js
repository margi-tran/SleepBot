/* eslint-disable semi */
'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

var verifyToken = function verifyToken(req, res, fbToken) {
  var fbTokenType = fbToken != null;
  var verifyTokenType = req != null && typeof req.query !== 'undefined' && typeof req.query['hub.verify_token'] !== 'undefined';

  if (fbTokenType && verifyTokenType) {
    if (req.query['hub.verify_token'] === fbToken) {
      res.status(200).send(req.query['hub.challenge']);
    } else {
      res.sendStatus(403);
    }
  } else {
    res.sendStatus(403);
  }
};

var ValidateWebhook = exports.ValidateWebhook = function () {
  function ValidateWebhook() {
    _classCallCheck(this, ValidateWebhook);
  }

  _createClass(ValidateWebhook, null, [{
    key: 'validate',
    value: function validate(req, res) {
      var fbToken = process.env.FB_VERIFICATION_TOKEN;
      verifyToken(req, res, fbToken);
    }
  }, {
    key: 'validateWithToken',
    value: function validateWithToken(req, res, token) {
      verifyToken(req, res, token);
    }
  }]);

  return ValidateWebhook;
}();